apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "itaysass-flask.fullname" . }}-test-connection"
  labels:
    {{- include "itaysass-flask.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "itaysass-flask.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
